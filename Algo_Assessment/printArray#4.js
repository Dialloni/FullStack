/*
Print Array Values printArrayVals(arr). Iterate through a given array, printing each value.
*/

function PrintArray(arr) {

    for ( var i = 0; i < arr.length; i++) {
        console.log(arr[i]);
    }
}
let names = ["Diallo", "Abshir", "Maverick", "Musa", "Khalid", "Hassan", "Abdul"];
PrintArray(names);